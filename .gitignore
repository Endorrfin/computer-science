# dependencies / build
node_modules
# CHANGED (S14): anchor to repo root so a bare "dist" can't ignore the new
# src/components/sims/dist/ source dir (ch.30 engines/components). Verified via
# git check-ignore — only the root build output + temp dist-s* dirs are ignored.
/dist
/dist-*

# reference assets — NEVER ship, NEVER commit (CLAUDE.md §10)
_examples/

# OS / IDE / logs
.DS_Store
.idea
.idea/

# stray sandbox test dir — not part of the site
# CHANGED: gitignore has no inline comments — a trailing "# …" makes the
# pattern literal and the dir was NOT ignored (verified via git check-ignore)
_test_ops/
_arhive/

*.log
npm-debug.log*
